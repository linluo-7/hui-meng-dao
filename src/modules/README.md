本目录用于按业务域拆分“页面逻辑/用例/组件组合”等（不直接放路由）。

- `auth/`：登录注册流程（验证码、协议校验等）
- `home/`：广场/推荐流
- `projects/`：企划广场/详情/创建编辑/审核/运营后台
- `roles/`：角色列表/详情/创建编辑
- `chat/`：戏群/水群/私信
- `me/`：个人中心
- `settings/`：设置/账号安全/隐私/通知
- `works/`：作品提交/详情

当前里程碑（M1）以页面骨架为主，后续会逐步把复杂逻辑从 `app/` 页面文件下沉到这里。

